spring.application.name=leave-management-service



# Port for this service
server.port=9003

# Eureka server location
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# MySQL Database Connection (A NEW database)
spring.datasource.url=jdbc:mysql://localhost:3306/smart_hr_leave_db
spring.datasource.username=root
spring.datasource.password=Devin@07
spring.jpa.hibernate.ddl-auto=update

spring.cloud.config.uri=http://localhost:8888

# --- THIS IS THE FIX ---
# This line is from the error message and enables the config client
spring.config.import=configserver:

# Optional: Tell it to fail fast if it can't find the config server on startup
spring.cloud.config.fail-fast=true

# --- Security & JWT ---
# This MUST be the same Base64 key as your other services
jwt.secret-key=aXNmZ3JlYXRIb25lc3RseU5vdEFTZWNyZXRQbGVhc2VSZXBsYWNlTWU=